création de bases de données:

CREATE DATABASE projet_php;

création tables:

CREATE TABLE villes (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nom VARCHAR(100) NOT NULL,
    pays VARCHAR(100) NOT NULL,
    capitale BOOLEAN DEFAULT FALSE
);

CREATE TABLE utilisateurs (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nom VARCHAR(100) NOT NULL,
    prenom VARCHAR(100) NOT NULL,
    pseudo VARCHAR(50) UNIQUE NOT NULL,
    mot_de_passe VARCHAR(255) NOT NULL,
    age INT NOT NULL,
    ville_id INT,
    FOREIGN KEY (ville_id) REFERENCES villes(id) ON DELETE SET NULL
);

nom des villes:

INSERT INTO villes (nom, pays, capitale) VALUES ('Bruxelles', 'Belgique', TRUE),
    -> ('Paris', 'France', TRUE),
    -> ('Londres', 'Royaume-Uni', TRUE),
    -> ('Berlin', 'Allemagne', TRUE),
    -> ('Madrid', 'Espagne', TRUE),
    -> ('Rome', 'Italie', TRUE),
    -> ('Tanger', 'Maroc', TRUE),
    -> ('Tokyo', 'Japon', TRUE),
    -> ('Amsterdam', 'Pays-Bas', TRUE),
    -> ('Lisbonne', 'Portugal', TRUE);



ALTER TABLE villes ADD nationalite VARCHAR(50);
UPDATE villes SET nationalite = 'Belge' WHERE pays = 'Belgique';
pour ajouter nationalite les 10 villes

ALTER TABLE villes ADD nationalite VARCHAR(50);
UPDATE villes SET nationalite = 'Japonaise' WHERE pays = 'Japon' AND set nationalite 'portugaise' WHERE pays = 'portugal';

